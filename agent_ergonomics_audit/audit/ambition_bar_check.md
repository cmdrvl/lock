# Ambition Bar Check

This focused full pass applied four high-leverage changes to a small artifact CLI that already had nested doctor surfaces:

- top-level mega-command: `lock --robot-triage`
- top-level capability contract: `lock capabilities --json`
- top-level agent guide: `lock robot-docs guide`
- safe refusal for `lock doctor --fix`
- release generator hardening for the Homebrew formula

The "That's it??" self-prompt was run before ending the pass. The remaining deferred class is generalized typo correction for arbitrary flags; this pass focused on making the first commands an agent naturally tries work directly and safely.
